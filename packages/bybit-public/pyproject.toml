[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "grid-bybit-public"
version = "0.2.0"
description = "Public-only Bybit V5 adapter with bounded retries and pagination"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
